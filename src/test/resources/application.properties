# 1. 애플리케이션 이름
spring.application.name=map-spring

# 2. DB 에러 방지 (H2 메모리 DB로 강제 설정)
# 이 설정을 넣어야 스프링이 "아, DB 여기 있구나" 하고 무사히 넘어갑니다.
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# 3. 메일 설정 (본인의 정보로 다시 확인)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=syjk7504@gmail.com
spring.mail.password=xpzusvkdavzjdnky
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 4. 중요: 메일 발송 과정 로그로 보기
# 메일이 왜 안 오는지 범인을 잡아줍니다.
spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=DEBUG

# 5. 기타 필수 설정 (에러 방지용)
spring.data.redis.host=localhost
spring.data.redis.port=6379
jwt.secret=testsecretkeytestsecretkeytestsecretkey
jwt.access-token-expiration=3600000
jwt.refresh-token-expiration=604800000